# Changelog

このファイルは公開版 RetroUX の変更履歴です。
形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準じます。

## [Unreleased]

## [1.0.3] - 2026-08-22

### 修正
- **`uv run pytest` が通るようになりました**。⚠ v1.0.0 から、開発側だけにある道具
  （調査用スクリプト・研究用の Lua ハーネス・内部の台帳）を検査するものを同梱して
  いたため、**1 件も走らずに止まっていました**（111 件が失敗）。それらを外し、
  ROM や FCEUX が要る検査は**無ければスキップ**するようにしました。
- ⚠ **セーブステートを読む処理が、非圧縮のものを読めていませんでした**。FCEUX は
  圧縮しても縮まないとき**そのまま**書きますが、その形に対応していませんでした。
  ★あわせて、検査の「材料があるか」の見張りを**入れ物の有無ではなく中身の数**で
  行うようにしました（絵が0枚・読めるセーブステートが0本のまま走って落ちていました）。
- **ゲームパッドの受け渡しを軽くしました**。エミュレータ側が入力ファイルを毎フレーム
  開き直していたのをやめ、1 回の読み取りが 147 分の 1 に（実測 16.2ms → 0.11ms）。
  ⚠ 倍速中に速度が落ちる・録画中にもたつく、が起きにくくなります。
- モンスターの絵の色: パレットを複数持つ 6 体（シドーなど）で、タイルごとのパレット
  番号を読んでいなかった（桃色と鮭色が入れ替わっていた）。スプライトのタイルを背景の
  前に描くようにした（16 体が実機と画素一致に）。
- 新規セットアップ（clone 直後）で世界地図が「歩いた範囲だけ」の縮尺になっていた
  （マップの大きさ表 `work\map-data\maps.json` が無いため）。初回起動時に ROM から
  自動生成するようにした。

### 追加
- **戦闘中に X を押しっぱなし**にすると、押している間だけ「強制オート ＋ 倍速」に
  なります（離すとすぐ戻り、倍速は**押す前の状態**へ戻ります）。入るまでの長さは
  `gamepad.force_auto_hold_ms`（既定 500ms）。⚠ ボスは対象外のままです。
- 地図ウィンドウの見出しの下に「🚪 いまの部屋」を1行表示（ダンジョンで立っている
  区画の番号とマス数。ROM の区画表から。区画の無いマップでは出ない）。

### 変更
- 世界地図の見せ方を `user_config.yaml` の `map.overworld_view` で選べるようにした
  （`walked` = 歩いた範囲を枠に収める〔既定。これまでと同じ見え方〕 / `full` = 256×256 の全体を
  倍率2で固定し自分中心にスクロール）。
- セーブステートのスロットに **0 も指定できる**ようになった（`shutdown.save_slot: 0`）。
  「0 は FCEUX が固まる」は誤りだった。
- 敵のデータ表（名前・ステータス・行動・ドロップ・経験値）を配布物から外し、
  起動時に**お手元の ROM から読む**ようにした（`work\generated\enemy_tables.json`
  にキャッシュ）。画面・自動戦闘の挙動は変わらない。ROM が無い環境では図鑑が
  「データがありません」表示になる（以前からの挙動）。

## [1.0.2] - 2026-08-21

### 追加
- `ARCHITECTURE.md`（全体構造・ファイルIPC・不変条件・機能追加の入口）と
  `CONTRIBUTING.md`（Issue/PR の作法。配布用ミラーである事情も明記）。

### 変更
- 戦闘AIの既定エンジンを `layered` に（戦況分析を判断に使用。戦術に反する
  行動を拒否層が止める）。画面の「戦況 ⚠ 試験中」表示は無くなります。
  従来どおりにしたい場合は `user_config.yaml` で `battle.engine: legacy`。

### 修正
- 戦闘が終わっても「戦闘中」のまま戻らないことがあった（勝利メッセージの
  画面IDがフィールドでも残留するため）。位置が動いたら戦闘終了とみなすようにした。
- 新規セットアップ（clone 直後）でモンスターの絵が1枚も表示されなかった。
  初回起動時に ROM から自動展開するようにした（`work\monster-art-rom\`）。

## [1.0.1] - 2026-08-20

### 追加
- ゲームパッドの右スティックでマウスカーソルを移動、R3（右スティック押し込み）で
  左クリック（押したままでドラッグ）。パッドだけで GUI のボタン等を操作できる。
  `gamepad.mouse: false` で無効化、速度は `gamepad.mouse_speed`（px/秒）。

### 修正
- ゲームパッド入力ファイルの毎フレーム I/O が turbo 戦闘の実効倍率を大きく下げていた
  （実測 ×3〜6 → 修正後 ×30 前後）。放置中に数秒おきに音がもたつく症状も同じ原因。
  未押下時の書き込みを 0.5 秒に1回へ間引き、エミュレータ側の読み取りを実時間
  （最大125Hz）でゲートした。
- turbo 中にパッドの押しっぱなしが誤って解除されることがあった（生存判定が
  フレーム数計上だったため。実時間 0.24〜0.5 秒に正常化）。

## [1.0.0] - 2026-08-XX

初回公開。

### 追加
- ファミコン版『ドラゴンクエストII』の自動プレイ補助（Windows / FCEUX 連携）。
- 戦闘の自動化（戦術プロファイル・キャラ別の役割）。
- まんたん（HP/MP 回復。ホイミ→ベホイミ→やくそう の優先）。
- 戦闘の倍速。
- 見た地図の可視化（歩いた範囲・ROM 由来のタイル）。
- セーブステートの世代バックアップ。
- XBOX ゲームパッド対応（挿すだけ。移動・基本操作・独自機能）。

> ⚠ ROM と FCEUX は同梱していません（各自で用意）。

[Unreleased]: https://github.com/akizuna515-bit/RetroUX/compare/v1.0.3...HEAD
[1.0.3]: https://github.com/akizuna515-bit/RetroUX/compare/v1.0.2...v1.0.3
[1.0.2]: https://github.com/akizuna515-bit/RetroUX/compare/v1.0.1...v1.0.2
[1.0.1]: https://github.com/akizuna515-bit/RetroUX/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/akizuna515-bit/RetroUX/releases/tag/v1.0.0
