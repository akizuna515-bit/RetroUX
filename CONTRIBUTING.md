# コントリビュートについて

Issue・Pull Request どちらも歓迎です。ただしこのリポジトリには
**少し変わった事情**があるので、先に1点だけ読んでください。

## ⚠ このリポジトリは「配布用ミラー」です

開発は別のリポジトリで行い、検査（ROM・秘密情報の混入チェック）を通した
ものをここへ書き出しています。そのため:

- **PR をこのリポジトリで直接 merge することはできません**
  （次回の同期で上書きされてしまうため）。
- いただいた PR は内容を開発側に取り込み、**次回のリリースで反映**します。
  反映時は CHANGELOG にお名前（GitHub ハンドル）を記載します。
- 取り込みまで時間がかかることがあります。ご了承ください。

フォークして自分の版を作るのは（MIT ライセンスの範囲で）ご自由にどうぞ。
面白い改造ができたら Issue で教えてもらえると嬉しいです。

## Issue（バグ報告・要望）

バグ報告に入れてほしいもの:

1. 何をしたら何が起きたか（期待と実際）
2. `work/retroux.log` の該当部分（時刻つき）
3. 環境: Windows のバージョン / FCEUX のバージョン（2.6.6 で動作確認しています）

⚠ ROM・セーブデータ・ゲーム画面の吸い出しデータは**添付しないでください**。

## Pull Request の作法

- **小さく**。1 PR = 1 目的。
- **テストを回す**: `uv run pytest`（ROM が無くても大半は動きます。
  ROM が要るものは自動で skip されます）。
- **新しい挙動にはテストを付ける**。このプロジェクトは「テストが仕様を
  日本語で語る」文化です（既存のテスト名を見てください）。
- **コメントには「なぜ」を書く**。何をしているかはコードが語るので、
  なぜそうしたか・何を実測したかを残してください。既存コードの
  ★/⚠ マーカーの流儀に合わせてもらえると馴染みます。
- **層の規則を守る**: 画面（`retroux/ui/`）から Win32 を直接呼ばない、
  `retroux/core/` から Qt を触らない、など。破るとテストが赤くなります。
- 改行コードは `.gitattributes` に従います（勝手に変換しない）。

全体の構造は [`ARCHITECTURE.md`](ARCHITECTURE.md) を先に読むのが近道です。

## やってはいけないこと

- ROM・セーブステート・ゲーム内の画像や音声データのコミット
  （`.gitignore` と公開前検査の両方で弾いていますが、そもそも入れないでください）
- `tools/`（エミュレータ置き場）・`work/`（実行時データ）を Git 管理に入れること

## 質問

Issue でどうぞ。日本語 / English どちらでも構いません（本文は日本語が中心です）。
